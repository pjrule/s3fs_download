#!/usr/bin/env python

from distutils.core import setup

setup(name='s3fs_download',
      version='0.1',
      description='Caching and downloading for the Dask s3fs library',
      author='Parker J. Rule',
      url='https://github.com/pjrule/s3fs_download',
      packages=['s3fs_download'],
      )
